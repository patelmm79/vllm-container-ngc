# Test Environment Variables
# Copy this file to .env.test and fill in your values

# Option 1: If you don't have gcloud installed locally, set these directly
# Get your service URL from: https://console.cloud.google.com/run
SERVICE_URL=https://vllm-deepseek-r1-1-5b-<hash>-uc.a.run.app

# Get auth token by running: gcloud auth print-identity-token
# (on a machine that has gcloud installed, or use Cloud Shell)
AUTH_TOKEN=your-identity-token-here

# Option 2: If you have gcloud installed, leave these commented out
# and the test will automatically fetch them
# SERVICE_URL=
# AUTH_TOKEN=
